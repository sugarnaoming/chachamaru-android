<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.sugarnaoming.chachamaru">

  <uses-permission android:name="android.permission.INTERNET" />

  <application
    android:allowBackup="true"
    android:icon="@drawable/ic_chachamaru_icon"
    android:label="@string/app_name"
    android:roundIcon="@drawable/ic_chachamaru_icon"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity
      android:name=".MainActivity"
      android:label="@string/app_name"
      android:theme="@style/ChachaTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".ControlViewRelation.GroupControlActivity" />
    <activity android:name=".ControlViewRelation.TabControlActivity" />
    <activity android:name=".GroupAddActivity" />
    <activity android:name=".GroupEditActivity" />
    <activity android:name=".TabAddActivity" />
    <activity android:name=".TabEditActivity"></activity>
  </application>

</manifest>